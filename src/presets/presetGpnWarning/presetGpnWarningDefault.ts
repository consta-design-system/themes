import './_color/Theme_color_gpnWarningDefault.css';
import './_color/Theme_color_gpnWarningDark.css';

import { ThemePreset } from '@consta/uikit/Theme';

export const presetGpnWarningDefault: ThemePreset = {
  color: {
    primary: 'gpnWarningDefault',
    accent: 'gpnWarningDark',
    invert: 'gpnWarningDark',
  },
  control: 'gpnDefault',
  font: 'gpnDefault',
  size: 'gpnDefault',
  space: 'gpnDefault',
  shadow: 'gpnDefault',
};
