import './_color/Theme_color_gpnWarningDefault.css';
import './_color/Theme_color_gpnWarningDark.css';

import { ThemePreset } from '@consta/uikit/Theme';

export const presetGpnWarningDark: ThemePreset = {
  color: {
    primary: 'gpnWarningDark',
    accent: 'gpnWarningDefault',
    invert: 'gpnWarningDefault',
  },
  control: 'gpnDefault',
  font: 'gpnDefault',
  size: 'gpnDefault',
  space: 'gpnDefault',
  shadow: 'gpnDefault',
};
