import './_color/Theme_color_gpnAlertDefault.css';
import './_color/Theme_color_gpnAlertDark.css';

import { ThemePreset } from '@consta/uikit/Theme';

export const presetGpnAlertDefault: ThemePreset = {
  color: {
    primary: 'gpnAlertDefault',
    accent: 'gpnAlertDark',
    invert: 'gpnAlertDark',
  },
  control: 'gpnDefault',
  font: 'gpnDefault',
  size: 'gpnDefault',
  space: 'gpnDefault',
  shadow: 'gpnDefault',
};
